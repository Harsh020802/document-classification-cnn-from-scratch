DROP AN IMAGE HERE, then from the project root run:

    ./run.sh scripts/predict_inbox.py

It picks up every image in this folder, predicts each one, and prints full stats.
Supported: .jpg .jpeg .png .tif .tiff .bmp .webp

Nothing here is committed to git.
